{
  "size": "light",
  "size_reason": "Wait detector on durable records. Widen the landed go-gate / ready-action owner. Full adversary rounds are a later ship if the light refuse is trusted.",
  "shape": "A node owns a next act. This seat proceeds without starting that act.",
  "form": "refuse-hook",
  "structure": "bin/fm-owner-invoke-wait-check.sh on map_next and date-cleared holds, and production ships[] gather for kind=ship metas whose session= matches state/.lock, with Stop ladder on data/<ov>/report.md then live endpoint then refuse, and report-gated data/<ov>/skills for plan-eng-review only when durable ov_harness is claude/claude*. Split transcript windows stay as gather holes. Does not parse brief-body Role or Delivery contract lines.",
  "rules": [
    "R-held-locked-next",
    "R-ov-missing",
    "R-skill-unloaded"
  ],
  "instances": [
    "Started Spec compile-check with no separate OV report. The builder's own plan note is not OV.",
    "Ship instructions never loaded plan-eng-review."
  ],
  "fixture": "reconstructed-historical",
  "fixture_artifact": "tests/fixtures/fm-owner-invoke-wait-check/",
  "fixture_source": {
    "date": "2026-08-22",
    "derivation": "reconstruct the 2026-08-22 sitting as durable ship and hold records"
  },
  "regression": {
    "test": "tests/fm-owner-invoke-wait-check.test.sh",
    "exact_counts": [1]
  },
  "ci_gate": ".github/workflows/ci.yml runs bin/fm-test-run.sh --check-coverage and all portable lanes; the test inventory includes tests/fm-owner-invoke-wait-check.test.sh and tests/fm-turnend-guard.test.sh",
  "structural_failure_exit": 2,
  "findings_exit": 1,
  "adversary_rounds": 0,
  "adversary_verdict": "skipped-light-size",
  "limits": [
    "Split transcripts can hide a load.",
    "A real captain hold is invisible.",
    "Empty or placeholder task fields skip ship rules.",
    "Builder self-review is not OV.",
    "ov= without report.md is not a review while the worker is gone.",
    "Non-Claude or missing ov_harness skips R-skill-unloaded (disclosed gap; those harnesses write no skills record)."
  ],
  "skipped": [
    "Adversarial rounds because size is light: one fresh reader via the test suite is enough.",
    "A skill-picker product.",
    "Changing Spec compile-check.",
    "Sibling role files on brief/spawn; that is another ship."
  ]
}

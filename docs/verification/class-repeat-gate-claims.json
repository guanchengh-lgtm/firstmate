{
  "size": "light",
  "form": "refuse-hook",
  "shape": "a fix was bound to one occurrence when the mechanism that produced it is shared",
  "structure": "home-local data/defect-classes.tsv binds miss-line phrasings to one class_id; bin/fm-teardown.sh matches the current miss against prior data/*/measure.md rows and requires an enforcing-file change on a second occurrence",
  "rules": [
    "class-repeat-second-occurrence"
  ],
  "fixture": "test-constructed",
  "fixture_generator": "",
  "regression": "tests/fm-teardown.test.sh class-repeat cases exercise refuse, enforcing-file allow, scout and none exemption, absent and invalid registry, and map_next non-escape through fm-teardown.sh",
  "ci_gate": ".github/workflows/ci.yml runs bin/fm-test-run.sh --check-coverage and all portable lanes; the test inventory includes tests/fm-teardown.test.sh",
  "structural_failure_exit": 1,
  "findings_exit": 1,
  "adversary_rounds": 0,
  "adversary_verdict": "skipped: lifecycle refuse-hook with bounded blast radius; light method (name, surface, rule, fail closed) plus regression tests",
  "limits": [
    "class membership is only as good as the captain-owned regex list",
    "an enforcing-file touch is not proof the mechanism covers the class",
    "none: measures and unregistered phrasing are invisible",
    "cross-home repeats are invisible"
  ],
  "skipped": [
    "derived historical fixture generator because the check is a teardown refuse-hook rather than a spec checker",
    "adversarial review rounds because the light method does not require them for this blast radius",
    "shipping data/defect-classes.tsv in firstmate git because the captain owns the vocabulary"
  ]
}

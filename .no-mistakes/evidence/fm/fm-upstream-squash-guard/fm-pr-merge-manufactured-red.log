ok - fm-pr-merge records pr= and pr_head= before invoking gh pr merge with head pin
ok - fm-pr-merge propagates a real merge failure without silently succeeding
ok - fm-pr-merge forwards extra flags to gh pr merge after the -- separator
ok - fm-pr-merge refuses before merging when task meta is missing
ok - fm-pr-merge refuses malformed PR URLs before calling the forge merge command
ok - fm-pr-merge refuses a well-formed GitLab merge request URL: GitHub-only by captain decision
ok - fm-pr-merge refuses unsafe PR URL segments before recording state
ok - fm-pr-merge refuses repo override args before recording state
ok - fm-pr-merge refuses a bundled short-option repo override and forwards other short flags
ok - fm-pr-merge does not add default --squash when the caller passes an explicit merge method
ok - fm-pr-merge translates --method=<value> to the gh shorthand without default --squash
ok - fm-pr-merge parses a GitHub PR URL into gh number and --repo arguments
ok - fm-pr-merge leaves GitHub extra-arg handling unchanged, including --sha
ok - fm-pr-merge refuses a red forge rollup before invoking gh pr merge
not ok - sync-branch-default: gh pr merge was not invoked as: pr merge 51 --repo example/repo --merge --match-head-commit 1010101010101010101010101010101010101010 (got: pr view https://github.com/example/repo/pull/51 --json headRefOid -q .headRefOid|pr view https://github.com/example/repo/pull/51 --json headRefOid,statusCheckRollup -q .headRefOid as $h | (.statusCheckRollup // [] | if length == 0 then "EMPTY" elif any(.[]; (.conclusion // .state // "") as $s | ($s=="FAILURE" or $s=="ERROR" or $s=="TIMED_OUT" or $s=="CANCELLED" or $s=="ACTION_REQUIRED")) then "RED" elif any(.[]; ((.status // "") != "COMPLETED") and ((.status // "") != "SKIPPED") and ((.state // "") != "SUCCESS")) then "PENDING" else "GREEN" end) as $v | "\($h) \($v)"|pr merge 51 --repo example/repo --squash --match-head-commit 1010101010101010101010101010101010101010|)

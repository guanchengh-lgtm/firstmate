not ok - records-before-merge: gh pr merge was not invoked as: pr merge 9 --repo example/repo --squash --match-head-commit deadbeefcafefeed0000000000000000deadbeef (got: pr view https://github.com/example/repo/pull/9 --json headRefName -q .headRefName|pr view https://github.com/example/repo/pull/9 --json headRefOid -q .headRefOid|pr view https://github.com/example/repo/pull/9 --json headRefOid,statusCheckRollup -q .headRefOid as $h | (.statusCheckRollup // [] | if length == 0 then "EMPTY" elif any(.[]; (.conclusion // .state // "") as $s | ($s=="FAILURE" or $s=="ERROR" or $s=="TIMED_OUT" or $s=="CANCELLED" or $s=="ACTION_REQUIRED")) then "RED" elif any(.[]; ((.status // "") != "COMPLETED") and ((.status // "") != "SKIPPED") and ((.state // "") != "SUCCESS")) then "PENDING" else "GREEN" end) as $v | "\($h) \($v)"|pr merge 9 --repo example/repo --squash|api graphql -f query=query($owner:String!,$repo:String!,$number:Int!){repository(owner:$owner,name:$repo){pullRequest(number:$number){state merged isInMergeQueue baseRefName}}} -F owner=example -F repo=repo -F number=9 --jq .data.repository.pullRequest | "state=" + (.state // ""), "merged=" + (.merged | tostring), "queued=" + (.isInMergeQueue | tostring), "base=" + (.baseRefName // "")|)
